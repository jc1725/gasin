# 쿠팡 보호 기간 정기 작업 상태

검증 시각은 **2026-08-14T14:16:09Z**이다. 쿠팡 오류에 명시된 보호 해제 시각인 2026-08-15T17:06:50Z 이전이므로, 가격·GoldBox 작업은 의도적으로 비활성 상태를 유지했다. 이 기간에는 추가 쿠팡 외부 호출을 만들지 않는다.

| 작업 | 주기(UTC) | 상태 | task UID | 운영 판단 |
| --- | --- | --- | --- | --- |
| 가격 갱신 | 매시 정각 | 비활성 | `hneRwpKGM5A3amomk48SMz` | 보호 해제 뒤 재개·로그 확인 필요 |
| GoldBox 수집 | 12시간마다 | 비활성 | `k3prYjqvg4hu4yavwTDyKj` | 보호 해제 뒤 재개·로그 확인 필요 |
| 이력 정리 | 매일 00:15 | 활성 | `Rk7QDnF8zJGa8aDzgEPaEM` | 쿠팡 API를 호출하지 않아 유지 |

가격·GoldBox 정기 작업은 새 전역 분당 30회 예산과 보호 모드가 배포된 상태에서만 재개한다. 재개 후에는 각 작업의 2xx 응답과 `syncRuns` 실행 로그를 별도로 확인한다.

## 보호 해제 후 재개 검증 예약

보호 해제보다 약 3분 늦은 **2026-08-15T17:10:00Z**에 재개 검증을 수행하도록 일회성 후속 작업을 활성화했다. 플랫폼 표시는 `Asia/Seoul` 시간대를 사용하며, 예약 표현식은 `0 10 2 16 8 *`이다. 이 작업은 해제 시각을 다시 확인한 뒤에만 가격·GoldBox 작업을 재개하고, 각 콜백의 2xx 응답 및 `syncRuns` 실행 결과를 이 문서에 추가 기록하도록 구성했다.

## 보호 해제 전 실행 이력

price 작업에는 2026-08-14T08:06:31Z에 발생한 과거 500 응답 1건이 남아 있다. 이 실행은 최신 보호 모드·콜백 예외 처리 배포 이전의 이력이므로, 현재 비활성 상태에서는 재시도하지 않는다. 보호 해제 뒤에는 새 price 실행이 **2xx**를 반환하고 `syncRuns`에 성공 기록을 남기는지 별도로 확인해야 한다. GoldBox 작업에는 보호 기간 이전의 실행 이력이 없다.

## 보호 해제 확인 및 작업 재개 기록

2026-08-15T17:13:06Z에 현재 시각이 오류 메시지의 보호 해제 시각(2026-08-15T17:06:50Z)을 지난 것을 확인했다. 운영 `searchApiQuotas`의 `coupang-products-search` 행도 `blockedUntil=2026-08-15 17:06:50`, `callCount=0`으로 조회되어 추가 보호 기간이 아닌 것을 확인했다.

그 후 price 작업(`hneRwpKGM5A3amomk48SMz`)과 GoldBox 작업(`k3prYjqvg4hu4yavwTDyKj`)을 재개했고, `manus-heartbeat list`에서 두 작업의 `is_enable=true` 상태를 확인했다. 이후 두 작업에서 딥링크 API의 `Coupang API error 400: url convert failed`가 반복되었고, `syncRuns`에는 2026-08-16T00:07:55Z GoldBox 1건과 2026-08-16T00:09:27Z~04:12:43Z price 5건이 모두 `failed`, `processedCount=0`으로 기록되었다.

원인은 기존 `link.coupang.com/re/...` 제휴 URL을 새 딥링크 API 입력으로 재전송한 흐름으로 분석했다. 이 URL은 이미 제휴 링크이므로 재생성하지 않고 저장 링크로 재사용하도록 수정했으며, 지원하지 않는 URL은 외부 호출 전에 `failed` 상태로 격리하도록 보강했다. 회귀 테스트와 전체 검사는 `pnpm check` 통과, 29개 테스트 파일 중 29개 통과·2개 스킵, 65개 테스트 통과·4개 스킵으로 확인했다.

추가 오류 호출을 막기 위해 2026-08-16T04:12:45Z에 price와 GoldBox 작업을 모두 일시 중지했다. 수정 버전 `e88e06b0` 배포와 `pnpm check`·`pnpm test` 통과 후 2026-08-16T04:28Z경 두 작업을 다시 활성화했다. 2026-08-16T05:06:08Z에 실행된 첫 수정 후 price 콜백은 **HTTP 200**으로 완료되었고, 운영 `syncRuns`에는 `status=success`, `processedCount=14`, `finishedAt=2026-08-16 05:06:16`이 기록되었다. 상세에는 기존 제휴 링크 20개 재사용, 딥링크 API 재호출 없음, GoldBox 승인 API 기반 가격 갱신, Google Drive 스냅샷 92개 갱신이 포함되어 있다. GoldBox 자체의 새 콜백은 12:00Z 실행 후 별도로 확인한다. 별도 검증용 쿠팡 호출은 만들지 않는다.
